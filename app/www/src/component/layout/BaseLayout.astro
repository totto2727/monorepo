---
import { css } from "~/style/pandacss/css";

import Favicon from "./meta/Favicon.astro";
import CloudflareWebAnalytics from "./script/CloudflareWebAnalytics.astro";

export interface Props {
  prefix?: string;
  title: string;
}

const { title, prefix } = Astro.props;
---

<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>{prefix ?? "totto2727 "}{title}</title>
    <meta charset="utf-8" slot="metas" />
    <Favicon />
    <slot name="meta" />
    <style>
      @layer reset, base, tokens, recipes, utilities;
    </style>
  </head>
  <body
    class={css({
      textStyle: "notoP",
      backgroundColor: "light.bg",
      color: "light.text",
      // backgroundColor: "dark.bg",
      // color: "dark.text",
    })}
  >
    <slot />
    <CloudflareWebAnalytics />
  </body>
</html>
