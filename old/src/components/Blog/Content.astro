---
import type { MicroCMSRichEditorField } from "@/config/microCMS";

export const blogContentIds = ["id", "title", "content"] as const;
export type BlogContentIds = typeof blogContentIds;
export type BlogProps = {
    content: MicroCMSRichEditorField;
};

export type Props = BlogProps;
const content = Astro.props.content;
---

<!-- <div class="blog-content-container" set:html={content} />

<style lang="scss" is:global>
    .blog-content-container {
        line-height: 1.9;

        > :first-child {
            margin-top: 0;
        }

        :is(h1, h2, h3, h4, h5, h6) {
            line-height: 1.5;
            margin-top: 1.5em;
            margin-bottom: 0.5em;
            padding-bottom: 0.25em;
        }
        :is(h1, h2) {
            border-bottom: solid 1px var(--border-base);
        }
        h1 {
            font-size: var(--size-h1);
        }
        h2 {
            font-size: var(--size-h2);
        }
        h3 {
            font-size: var(--size-h3);
        }
        h4 {
            font-size: var(--size-h4);
        }
        h5 {
            font-size: var(--size-h5);
        }
        h6 {
            font-size: var(--size-h6);
        }
    }
</style> -->
